const { chromium } = require('playwright'); async function run() { const browser = await chromium.launch({headless:true}); const context = await browser.newContext(); const page = await context.newPage(); page.on('request', req => { if (req.url().includes('m3u8')) { console.log('M3U8 REQUEST HEADERS:', req.headers()); } }); await page.goto('https://embedindia.st/embed/afle/2026-08-16/lon-pan'); await page.waitForTimeout(5000); await browser.close(); } run();
