const { chromium } = require('playwright'); async function run() { const browser = await chromium.launch({headless:true, args: ['--disable-blink-features=AutomationControlled']}); const context = await browser.newContext({userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/127.0.0.0 Safari/537.36'}); const page = await context.newPage(); page.on('request', req => { if (req.url().includes('m3u8')) { console.log('M3U8 REQUEST HEADERS:', req.headers()); } }); await page.goto('https://embedindia.st/embed/afle/2026-08-16/lon-pan'); await page.waitForTimeout(10000); await browser.close(); } run();
