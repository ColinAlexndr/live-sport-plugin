const fetch = require('node-fetch'); async function run() { const B = require('./src/providers/BinTvProvider.js'); const b = new B({circuitBreaker:{wrap:(n,f)=>({fire:f})}}); const m = await b.getMatches(); const id = m[0].sources[0].id; const s = await b.resolveStream(id); const url = s[0].url; console.log(url); const r = await fetch(url, { headers: { 'User-Agent': 'Node.js/22' } }); console.log('Status:', r.status); console.log(await r.text()); } run();
