const fetch = require('node-fetch'); async function run() { const B = require('./src/providers/BinTvProvider.js'); const b = new B({circuitBreaker:{wrap:(n,f)=>({fire:f})}}); const m = await b.getMatches(); const id = m[0].sources[0].id; const s = await b.resolveStream(id); const url = s[0].url; console.log(url); const r = await fetch(url, { headers: { 'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/127.0.0.0 Safari/537.36', 'Accept': '*/*', 'Accept-Language': 'en-US,en;q=0.9', 'Origin': 'https://embedindia.st', 'Referer': 'https://embedindia.st/' } }); console.log('Status:', r.status); } run();
