const fetch = require('node-fetch'); const B = require('./src/providers/BinTvProvider.js'); const b = new B({circuitBreaker:{wrap:(n,f)=>({fire:f})}}); b.getMatches().then(m => { const id = m[0].sources[0].id; b.resolveStream(id).then(async s => { const url = s[0].url; console.log('Fresh URL:', url); const res = await fetch(url, { headers: { 'Origin': 'https://embedindia.st', 'Referer': 'https://embedindia.st/', 'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/127.0.0.0 Safari/537.36' } }); console.log('Fetch status:', res.status); console.log('Fetch body snippet:', (await res.text()).slice(0, 200)); }); });
