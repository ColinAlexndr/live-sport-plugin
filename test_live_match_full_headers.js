const fetch = require('node-fetch'); async function run() { const B = require('./src/providers/BinTvProvider.js'); const b = new B({circuitBreaker:{wrap:(n,f)=>({fire:f})}}); const m = await b.getMatches(); const liveMatch = m.find(match => parseInt(match.date) < Date.now()); const id = liveMatch.sources[0].id; const s = await b.resolveStream(id); const url = s[0].url; console.log('Extracted URL:', url); const r = await fetch(url, { headers: { 'User-Agent': b.userAgent, 'Accept': '*/*', 'Accept-Language': 'en-US,en;q=0.9', 'Accept-Encoding': 'gzip, deflate, br', 'Origin': 'https://embedindia.st', 'Referer': 'https://embedindia.st/', 'Connection': 'keep-alive', 'Sec-Fetch-Dest': 'empty', 'Sec-Fetch-Mode': 'cors', 'Sec-Fetch-Site': 'cross-site' } }); console.log('Status:', r.status); } run();
