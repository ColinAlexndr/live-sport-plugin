const { chromium } = require('playwright'); async function run() { const browser = await chromium.launch({headless: true}); const context = await browser.newContext({ userAgent: 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/127.0.0.0 Safari/537.36', }); const page = await context.newPage(); page.on('response', async res => { if (res.url().includes('indianservers.st')) { console.log('Got response from:', res.url(), res.status()); if (res.url().includes('m3u8')) { console.log('M3U8 body:', await res.text()); } } }); await page.goto('https://embedindia.st/embed/afle/2026-08-16/lon-pan'); await page.waitForTimeout(10000); const html = await page.content(); console.log('Title:', await page.title()); await browser.close(); } run();
