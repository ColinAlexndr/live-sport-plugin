const fetch = require('node-fetch'); const cheerio = require('cheerio'); fetch('https://sportyhunter.xyz').then(r=>r.text()).then(t => { const $ = cheerio.load(t); const nextData = cheerio.load(t)('#__NEXT_DATA__').html(); if (nextData) { const data = JSON.parse(nextData); if(data.props.pageProps && data.props.pageProps.matches && data.props.pageProps.matches.length > 0) { console.log(JSON.stringify(data.props.pageProps.matches[0], null, 2)); } else { console.log('No matches'); } } else { console.log('No NEXT DATA'); } });
