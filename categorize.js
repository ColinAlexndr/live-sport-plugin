const fs = require('fs'); const path = require('path'); const files = fs.readdirSync('./src/providers').filter(f => f.endsWith('Provider.js') && f !== 'BaseProvider.js'); files.forEach(f => { const content = fs.readFileSync(path.join('./src/providers', f), 'utf8'); const hasDirect = content.includes('entityParams.url =') || content.includes(' url: ') || content.includes(' url = '); const hasExternal = content.includes('externalUrl:'); console.log(f.padEnd(30), 'Direct:', hasDirect, 'External:', hasExternal); });
