const { chromium } = require('playwright'); const fs = require('fs'); (async () => { const browser = await chromium.launch(); const context = await browser.newContext({ acceptDownloads: true }); const page = await context.newPage(); await page.goto('about:blank'); console.time('dl'); const [download] = await Promise.all([page.waitForEvent('download'), page.evaluate(() => { const a = document.createElement('a'); a.href = 'https://shiva.indianservers.st/secure/dUzOcllXItSdlEBwZsJWTwImQrDIHBmn/1787122800/1787169600/test_eng/index.m3u8'; a.download = 'test'; a.click(); })]); const path = await download.path(); const data = fs.readFileSync(path); console.log(data.length); await download.delete(); console.timeEnd('dl'); await browser.close(); })();
